CREATE TABLE mediaListEntity (
    list_name TEXT PRIMARY KEY NOT NULL
);

insert:
INSERT OR REPLACE
INTO mediaListEntity(
    list_name
) VALUES (?);

list:
SELECT *
FROM mediaListEntity
WHERE list_name = ?;

allLists:
SELECT *
FROM mediaListEntity;